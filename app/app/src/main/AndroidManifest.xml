<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comp6239">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".AppointmentApproveActivity"></activity>
        <activity android:name=".TutorDetailActivity" />
        <activity android:name=".StudentChatActivity" />
        <activity android:name=".TutorChatActivity" />
        <activity android:name=".AdminAproveTutorActiviry" />
        <activity android:name=".TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TutorAdjustActivity" />
        <activity android:name=".TutorRequestActivity" />
        <activity android:name=".TutorMainActivity" />
        <activity android:name=".StudentProfileActivity" />
        <activity android:name=".StudentMainActivity" />
        <activity
            android:name=".StudentSearchActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".AdminMainActivity"
            android:label="@string/title_admin_portal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".StudentRegisterActivity"
            android:label="@string/title_activity_student_register" />
        <activity
            android:name=".TutorRegisterActivity"
            android:label="@string/title_activity_tutor_register" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>